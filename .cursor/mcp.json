{
  "mcpServers": {
    "flutter-dart": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--volume", "${workspaceFolder}:/workspace",
        "flutter-dart-mcp:latest"
      ]
    }
  }
}
